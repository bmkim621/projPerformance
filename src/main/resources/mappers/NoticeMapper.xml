<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yi.mapper.NoticeMapper">

	<!-- 공지사항 리스트 -->
	<select id="noticeListAll" resultType="NoticeVO">
		select notice_no, content, title, regdate, is_notice, view_cnt, notice_category, manager_code, writer  
		from notice;
	</select>
	
	<!-- 직원 -->
	<select id="selectManagerByCode" resultType="ManagerVO">
		select manager_code, manager_name from manager
		where manager_code = #{managerCode};
	</select>
</mapper>